/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "media.licdn.com",
      "avatars.githubusercontent.com",
      "cdn.jsdelivr.net",
    ],
  },
};

module.exports = nextConfig;
